AJS.toInit(function(){
   require(['aui/flag'], function(flag) {
       var myFlag = flag({
           type: 'success',
           title: 'Release Planning Plugin',
           body: 'JS Controller is working'
       });
   });
});
